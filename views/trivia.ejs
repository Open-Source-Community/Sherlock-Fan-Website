<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    

    <% for(var i=0; i<objects.length ; i++) { %>

        <!-- question  -->
        
        <%= objects[i].question %>


            <!-- options  -->
            <br>
       <% for(var j=0 ; j<3;j++){ %>
         

        
        <% if(objects[i].options[j]==objects[i].correctAnswer ) { %>
              <button onclick="this.style.backgroundColor='green' ">  <%=objects[i].options[j] %>  </button>

         <% } else { %>
            <button onclick="this.style.backgroundColor='red' ">  <%=objects[i].options[j] %>  </button>
            <%}%>

        <%}%>  
        <br>

            
              <!-- correct answer  -->
              <div style="display: none; background-color: green;">
                  <%= objects[i].correctAnswer %>
                </div>
        
    <%}%>

</body>
</html>